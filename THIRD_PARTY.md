# Third-party material

`vendor/wireguard-tools/wireguard-tools-1.0.20260223.tar.xz` is the
unmodified upstream WireGuard tools release source. WireGuard tools is licensed
under GPL-2.0; its source archive contains the upstream license and notices.

The corresponding upstream project is <https://git.zx2c4.com/wireguard-tools/>.
The adjacent detached signature and checksum are retained to authenticate the
vendored archive. See `vendor/README.md` for provenance and verification.

The project-level public-domain dedication in `LICENSE` does not replace or
alter any third-party license.
